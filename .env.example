# API Keys
GEMINI_API_KEY="api key"
GROQ_API_KEY="api key"

# HuggingFace (free account — get token at https://huggingface.co/settings/tokens)
# Used for CogVideoX-2B AI video generation (1000 free requests/month).
# If not set, AI video scenes fall back to Pollinations.ai images automatically.
HF_TOKEN=your_huggingface_token_here

# Pexels & Pixabay (both free, no credit card, ~2 minutes to sign up) — real stock
# photos AND video b-roll that actually match the script's subject, used before the
# DDG-scraping fallback (which gets rate-limited under normal automated use) and
# before Pollinations.ai AI image generation. Optional: everything still works
# without these, just with less reliable/relevant visuals.
#   Pexels:  https://www.pexels.com/api/
#   Pixabay: https://pixabay.com/api/docs/
PEXELS_API_KEY=your_pexels_api_key_here
PIXABAY_API_KEY=your_pixabay_api_key_here

# Visual relevance check (uses your existing GEMINI_API_KEY — no extra signup): before
# accepting a fetched photo/video, Gemini's vision is asked whether it actually matches
# the scene's narration; if nothing fetched passes, an AI image is generated from the
# narration instead. Fails open (never blocks the pipeline) if the check itself errors.
# Uncomment to turn it off entirely:
# DISABLE_VISUAL_RELEVANCE_CHECK=true

# Telegram (Optional - for Telegram Fetcher)
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash

# YouTube (Handled via client_secrets.json and token.pickle)
# You will need to run the script locally once to generate token.pickle
